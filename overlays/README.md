Overlay categories used by the image builder live here.

- `common/` contains support overlays shared across image profiles.
- `firmware-extended/` contains the selected imported feature overlays.
- `devel/` is reserved for development-only overlays.
